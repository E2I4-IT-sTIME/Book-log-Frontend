import type { NextPage } from "next";

const Home: NextPage = () => {
  //메인페이지
  return <>
    <div>
    </div>
    <style global jsx>{`
      div {
         height: calc(100% - 50px);
      }
      `}</style>

  </>;
};

export default Home;
