interface setter {
  setContent: (value: string) => void;
  setGenres: (value: string) => void;
}

export default function MakeClubDown(props: setter) {
  const { setContent, setGenres } = props;

  return <></>;
}
